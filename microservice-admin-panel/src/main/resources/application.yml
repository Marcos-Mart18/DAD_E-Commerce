spring:
  application:
    name: msvc-admin-panel
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8100

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
